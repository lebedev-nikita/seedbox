# AGENTS.md

## React rules

- Each component should be placed in it's own file
- Each component should have optional `className` prop
- All external positioning must be passed via `className` prop (like a modifier in BEM methodology)
